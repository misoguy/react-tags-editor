import React, { Component, PropTypes } from 'react';

export default class ReactTagsEditor extends Component {
  render() {
    return (
      <div>
        REACT TAGS EDITOR!!
      </div>
    );
  }
};